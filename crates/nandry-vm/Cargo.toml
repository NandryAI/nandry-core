[package]
name = "nandry-vm"
version.workspace = true
edition.workspace = true
license.workspace = true

[features]
default = ["std"]
std = []

[dev-dependencies]
nandry-ir = { path = "../nandry-ir" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints]
workspace = true
